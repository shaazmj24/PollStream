
LEARN: 


From Poll.html:
//use AJAX to avoid invalid response error (return not written) 
                    //send data in the background to flask and either stay on the same page unless result button is clicked or to new page 
                    fetch('/result', { 
                        method: 'POST', 
                        headers: { 'Content-Type': 'application/x-www-form-urlencoded' },      //tells flask we sendin html format so it will treat it like a format
                        body: 'vote=' + encodeURIComponent(selectedValue) + 
                              '&identity=' + encodeURIComponent(id)
                    }); 
                
                                   to 
                                   this 
            
From APP:
@app.route('/result', methods=['POST'])
def voters():  
    identity = request.form.get('identity')
    option = request.form.get('vote')
    #make new session if not created yet (global variable)
    if identity in session: 
        choice_map = session[identity]
    else:   
        choice_map = {}

    if (option == "none"):  
        choice, selected = None, None
    else:  
        selected, choice = option.split("&")  
        if choice in choice_map:  
            choice_map[choice] += 1
        else: 
            choice_map[choice] = 1 

    #save updated map back to session 
    session[identity] = choice_map      #like session[#code#, variable]  
    return '', 204                          #return silent/empty response with 204 to avoid invalid response 
                                            #error and stay on the same page(poll). ideal for using AJAX JS

    